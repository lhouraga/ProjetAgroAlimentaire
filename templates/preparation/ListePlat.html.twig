{% extends 'base.html.twig' %}

{% block body %}

<div class="container mt-4">
<h1> Liste des plats </h1>
<div class="row">
<div class="col-md-8">
<table class="table table-striped">
<thead>
<tr>
    <th> Nom du plat</th>
    <th>Nombre de fab</th>
    <th>Date de preparation</th>
    <th>Total</th>
    <th>Action souhaitée</th>
</tr>
</thead>
<tbody>
  
 {%   for plat in plats %}
   <tr> 
      <td>{{ plat.NomPlat}} </td>
       <td>{{ plat.NbrePlat}} </td>
       <td>{{ plat.DatePrepare |date('d/m/Y')}} </td>
       <td>{{ plat.prixTotal}} </td>
      <td>
          <div class="float-right">
          <a href="{{path('afficherPlat', {id: plat.id})}}" class="btn btn-success"> Voir Composition </a>
          
          </div>
       </td> 
   
  </tr>
  {% endfor %}
</tbody>
</table>  
 </div>
 </div> 
</div>

{% endblock %}